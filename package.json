
{
  "name": "remind-pro",
  "version": "1.0.0",
  "description": "Professional Task Manager for iOS",
  "main": "index.tsx",
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "scripts": {
    "build": "vite build",
    "cap": "cap",
    "add-ios": "npx cap add ios",
    "sync": "npm run build && npx cap sync ios",
    "open": "npx cap open ios"
  }
}
